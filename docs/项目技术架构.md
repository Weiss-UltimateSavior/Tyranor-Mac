# Galgame 模拟器 macOS 架构与技术栈计划

> 项目定位：一个面向 macOS 的原生 Galgame 模拟器。
>
> 当前计划支持的游戏引擎：
>
> * KR
> * AR
> * ONS
>
> 核心技术路线：
>
> **SwiftUI + AppKit + Rust + C++ + CXX + UniFFI + wgpu + Metal + AVFoundation + Cargo + CMake + Swift Package Manager**
>
> 项目核心设计：
>
> **Swift 负责 macOS 应用层，Rust 负责模拟器核心，C++ 负责底层原生库复用，wgpu/Metal 负责图形渲染。**

---

# 一、项目目标

## 1.1 总体目标

开发一个原生 macOS Galgame 模拟器，用于运行基于：

```text
KR
AR
ONS
```

三个目标引擎的游戏。

模拟器主要负责：

```text
游戏识别
游戏加载
资源解析
脚本解析
VM / Runtime
文本处理
图片处理
音频播放
视频播放
输入处理
场景管理
游戏存档
GPU 渲染
窗口管理
```

macOS 应用层负责：

```text
游戏库
游戏选择
游戏信息
设置
窗口
菜单
快捷键
文件选择
存档管理
模拟器控制
```

---

# 二、总体架构目标

项目采用：

```text
macOS Frontend
        │
        ▼
Emulator Controller
        │
        ▼
Rust Emulator Core
        │
        ├── KR Engine
        ├── AR Engine
        ├── ONS Engine
        │
        ├── VM
        ├── Resource System
        ├── Renderer
        ├── Audio
        ├── Input
        └── Save
        │
        ├──────────────┐
        ▼              ▼
      C++            wgpu
        │              │
        ▼              ▼
    Native Libs       Metal
```

核心要求：

> UI 与模拟器内核完全解耦。

> Engine 与 macOS 平台完全解耦。

> VM 不允许直接依赖 Swift / AppKit。

> C++ 作为底层库存在，不作为整个模拟器的总控制层。

---

# 三、技术栈

| 层级           | 技术                           | 用途                        |
| ------------ | ---------------------------- | ------------------------- |
| macOS UI     | SwiftUI                      | 主界面、游戏库、设置                |
| macOS 原生 API | AppKit                       | Window、Menu、快捷键、文件系统、生命周期 |
| 模拟器核心        | Rust                         | Emulator、Engine、VM、资源、运行时 |
| C++          | C++17 / C++20                | 原生解析库、底层算法、已有代码复用         |
| Rust ↔ C++   | CXX                          | Rust/C++ FFI              |
| Rust ↔ Swift | UniFFI                       | Swift 调用 Rust             |
| GPU          | wgpu                         | 跨平台 GPU 抽象                |
| macOS GPU    | Metal                        | Apple GPU 后端              |
| macOS 游戏画面   | MTKView / CAMetalLayer       | 游戏画面承载                    |
| 音频           | AVAudioEngine / AudioToolbox | BGM、SE、Voice              |
| 视频           | AVFoundation                 | 游戏视频                      |
| 视频备用         | FFmpeg                       | 特殊视频格式                    |
| Rust 构建      | Cargo                        | Rust 工程                   |
| C++ 构建       | CMake                        | C++ 工程                    |
| Swift 构建     | SwiftPM / Xcode              | macOS App                 |
| Rust 测试      | cargo test                   | Core 测试                   |
| Swift 测试     | XCTest                       | UI / Controller 测试        |
| C++ 测试       | GoogleTest                   | C++ 底层库测试                 |
| CI           | GitHub Actions               | 自动构建和测试                   |

---

# 四、为什么选择 Rust 作为模拟器核心

Rust 是整个项目的核心语言。

原因：

```text
安全的内存管理
+
较低的运行时开销
+
C/C++ FFI 能力
+
跨平台能力
+
适合 VM / Parser / Runtime
+
适合 wgpu
```

最终期望：

```text
Rust Core
    │
    ├── macOS
    ├── Windows
    ├── Linux
    └── Android
```

因此核心代码尽量不能依赖 macOS。

---

# 五、为什么 macOS UI 使用 SwiftUI

项目是 macOS 原生应用，因此 UI 优先采用：

```text
SwiftUI
```

负责：

```text
Game Library
Game Detail
Settings
Save Manager
About
Toolbar
Sidebar
```

但是不会完全放弃 AppKit。

AppKit 负责：

```text
Window
Menu
Keyboard Shortcut
File Dialog
Drag & Drop
Window Lifecycle
Native macOS Integration
```

最终采用：

```text
SwiftUI
+
AppKit
```

而不是单独使用其中一个。

---

# 六、为什么不使用 Electron / Flutter / Tauri

本项目本质是：

> 模拟器，而不是普通桌面 CRUD 应用。

模拟器需要大量处理：

```text
GPU
输入
窗口
声音
视频
高频帧更新
游戏循环
原生库
```

因此：

```text
Electron
Flutter
Tauri
```

并不是本项目的第一选择。

这些框架可以做游戏管理器，但会增加：

```text
Web / Dart / JS
↓
Native Bridge
↓
Rust / C++
```

等额外层级。

当前项目直接采用：

```text
SwiftUI
    ↓
Rust
```

结构更加直接。

---

# 七、项目目录

采用 Monorepo。

推荐目录：

```text
GalEmu/
│
├── macos/
│   └── GalEmu/
│       │
│       ├── App/
│       │   ├── GalEmuApp.swift
│       │   └── AppDelegate.swift
│       │
│       ├── Views/
│       │   ├── ContentView.swift
│       │   ├── GameLibraryView.swift
│       │   ├── GameDetailView.swift
│       │   ├── EmulatorView.swift
│       │   ├── SettingsView.swift
│       │   └── SaveManagerView.swift
│       │
│       ├── Controllers/
│       │   ├── EmulatorController.swift
│       │   ├── GameLibraryController.swift
│       │   └── InputController.swift
│       │
│       ├── Models/
│       │   ├── Game.swift
│       │   ├── GameMetadata.swift
│       │   └── EmulatorSettings.swift
│       │
│       └── Resources/
│
├── core/
│   ├── Cargo.toml
│   │
│   └── src/
│       ├── lib.rs
│       │
│       ├── emulator/
│       │   ├── mod.rs
│       │   ├── emulator.rs
│       │   ├── runtime.rs
│       │   └── state.rs
│       │
│       ├── engine/
│       │   ├── mod.rs
│       │   ├── engine.rs
│       │   ├── manager.rs
│       │   │
│       │   ├── common/
│       │   │   ├── mod.rs
│       │   │   ├── types.rs
│       │   │   ├── command.rs
│       │   │   └── state.rs
│       │   │
│       │   ├── kr/
│       │   │   ├── mod.rs
│       │   │   ├── detector.rs
│       │   │   ├── loader.rs
│       │   │   ├── parser.rs
│       │   │   ├── vm.rs
│       │   │   ├── opcode.rs
│       │   │   ├── resource.rs
│       │   │   └── renderer.rs
│       │   │
│       │   ├── ar/
│       │   │   ├── mod.rs
│       │   │   ├── detector.rs
│       │   │   ├── loader.rs
│       │   │   ├── parser.rs
│       │   │   ├── vm.rs
│       │   │   ├── opcode.rs
│       │   │   ├── resource.rs
│       │   │   └── renderer.rs
│       │   │
│       │   └── ons/
│       │       ├── mod.rs
│       │       ├── detector.rs
│       │       ├── loader.rs
│       │       ├── parser.rs
│       │       ├── vm.rs
│       │       ├── opcode.rs
│       │       ├── resource.rs
│       │       └── renderer.rs
│       │
│       ├── vm/
│       │   ├── mod.rs
│       │   ├── instruction.rs
│       │   ├── stack.rs
│       │   ├── memory.rs
│       │   ├── variable.rs
│       │   └── context.rs
│       │
│       ├── resource/
│       │   ├── mod.rs
│       │   ├── filesystem.rs
│       │   ├── archive.rs
│       │   ├── image.rs
│       │   ├── text.rs
│       │   ├── audio.rs
│       │   ├── video.rs
│       │   └── font.rs
│       │
│       ├── renderer/
│       │   ├── mod.rs
│       │   ├── renderer.rs
│       │   ├── device.rs
│       │   ├── texture.rs
│       │   ├── buffer.rs
│       │   ├── shader.rs
│       │   └── command.rs
│       │
│       ├── audio/
│       │   ├── mod.rs
│       │   ├── audio.rs
│       │   ├── channel.rs
│       │   └── command.rs
│       │
│       ├── input/
│       │   ├── mod.rs
│       │   ├── event.rs
│       │   └── mapper.rs
│       │
│       ├── save/
│       │   ├── mod.rs
│       │   ├── save.rs
│       │   └── state.rs
│       │
│       └── ffi/
│           ├── mod.rs
│           └── api.rs
│
├── cpp/
│   ├── CMakeLists.txt
│   │
│   ├── common/
│   ├── kr/
│   ├── ar/
│   ├── ons/
│   ├── parser/
│   ├── image/
│   └── audio/
│
├── shaders/
│   ├── sprite.wgsl
│   ├── text.wgsl
│   ├── background.wgsl
│   └── postprocess.wgsl
│
├── tests/
│   ├── engine/
│   ├── vm/
│   ├── resource/
│   └── integration/
│
├── testdata/
│   ├── kr/
│   ├── ar/
│   └── ons/
│
├── scripts/
│   ├── build.sh
│   ├── build_cpp.sh
│   ├── build_rust.sh
│   └── package_app.sh
│
├── Cargo.toml
├── Cargo.lock
├── CMakeLists.txt
├── README.md
├── ARCHITECTURE.md
└── LICENSE
```

---

# 八、核心模块

Rust Core 主要分为：

```text
Emulator
Engine
VM
Resource
Renderer
Audio
Input
Save
FFI
```

依赖关系：

```text
                    Emulator
                       │
       ┌───────────────┼────────────────┐
       │               │                │
       ▼               ▼                ▼
    Engine            VM            Resource
       │               │                │
       └───────────────┼────────────────┘
                       │
             ┌─────────┴─────────┐
             ▼                   ▼
          Renderer             Audio
             │                   │
             └─────────┬─────────┘
                       ▼
                     Input
```

---

# 九、Emulator Runtime

`Emulator` 是整个 Rust Core 的顶层对象。

负责：

```text
初始化
加载游戏
运行
暂停
继续
停止
重置
关闭
保存
读取
```

概念结构：

```text
Emulator
├── EngineManager
├── Renderer
├── AudioEngine
├── InputSystem
├── ResourceManager
├── SaveManager
└── RuntimeState
```

生命周期：

```text
Created
   ↓
Initialized
   ↓
GameLoaded
   ↓
Running
   │
   ├── Paused
   │
   └── Running
   ↓
Stopped
   ↓
Destroyed
```

---

# 十、Engine Manager

当前只支持：

```text
KR
AR
ONS
```

Engine Manager：

```text
EngineManager
├── KR Engine
├── AR Engine
└── ONS Engine
```

启动游戏：

```text
Game Path
   ↓
Engine Detector
   ↓
EngineType
   ↓
EngineManager
   ↓
对应 Engine
```

---

# 十一、Engine 抽象

三个引擎共享统一的生命周期接口。

逻辑接口：

```text
Engine
├── detect()
├── load()
├── initialize()
├── update()
├── render()
├── input()
├── save()
├── load_state()
└── shutdown()
```

具体实现：

```text
KR Engine
AR Engine
ONS Engine
```

但这里要注意：

> Engine 抽象只统一生命周期，不强迫三个引擎内部实现方式完全一致。

例如 KR、AR、ONS 的：

```text
脚本格式
资源格式
VM
场景模型
渲染规则
存档
```

可以完全不同。

公共层只提供真正必要的共同能力。

---

# 十二、KR Engine

目录：

```text
engine/kr/
```

负责：

```text
KR 游戏识别
KR 游戏加载
KR 资源解析
KR 脚本解析
KR VM
KR Opcode
KR 渲染
KR 音频
KR 输入
KR 存档
```

结构：

```text
KR Engine
├── Detector
├── Loader
├── Parser
├── VM
├── Opcode
├── Resource
└── Renderer
```

所有 KR 特有逻辑尽可能限制在此目录。

---

# 十三、AR Engine

目录：

```text
engine/ar/
```

负责：

```text
AR 游戏识别
AR 游戏加载
AR 资源解析
AR 脚本解析
AR VM
AR Opcode
AR 渲染
AR 音频
AR 输入
AR 存档
```

结构：

```text
AR Engine
├── Detector
├── Loader
├── Parser
├── VM
├── Opcode
├── Resource
└── Renderer
```

---

# 十四、ONS Engine

目录：

```text
engine/ons/
```

负责：

```text
ONS 游戏识别
ONS 游戏加载
ONS 资源解析
ONS 脚本解析
ONS VM
ONS Opcode
ONS 渲染
ONS 音频
ONS 输入
ONS 存档
```

结构：

```text
ONS Engine
├── Detector
├── Loader
├── Parser
├── VM
├── Opcode
├── Resource
└── Renderer
```

---

# 十五、为什么三个 Engine 必须独立

不要写成：

```text
Engine
├── if KR
├── if AR
├── if ONS
```

然后所有代码混在一起。

应该：

```text
Engine Manager
    │
    ├── KR
    │
    ├── AR
    │
    └── ONS
```

原因：

```text
降低耦合
方便调试
方便测试
避免条件分支污染核心
方便未来增加新引擎
```

---

# 十六、VM 架构

VM 负责执行游戏脚本。

建议抽象：

```text
VM
├── Program Counter
├── Stack
├── Variables
├── Memory
├── Call Stack
└── Runtime Context
```

脚本流程：

```text
Game Script
    ↓
Parser
    ↓
Instruction
    ↓
VM
    ↓
Opcode Dispatcher
    ↓
Runtime Command
```

例如：

```text
ShowText
PlayBGM
ShowImage
Wait
Jump
Call
Return
```

---

# 十七、VM 与平台隔离

绝对避免：

```text
KR VM
   ↓
Swift
```

或者：

```text
AR VM
   ↓
AppKit
```

正确结构：

```text
VM
 ↓
Runtime Command
 ↓
Subsystem
 ↓
平台 Backend
```

例如：

```text
VM
 ↓
PlayBGM
 ↓
Audio Command
 ↓
macOS Audio Backend
```

这样未来换平台时 VM 不需要修改。

---

# 十八、公共 Runtime

三个引擎可以使用一部分公共 Runtime。

例如：

```text
engine/common/
```

可以保存：

```text
EngineType
GameInfo
RenderCommand
AudioCommand
InputEvent
RuntimeState
Scene
Sprite
Text
Transform
```

但不要为了“代码复用”强行抽取引擎私有数据。

原则：

> 真正公共的才进入 common。

---

# 十九、Command 系统

建议内部建立 Command 模型。

例如：

```text
RenderCommand
├── DrawSprite
├── DrawText
├── DrawBackground
├── Clear
└── Present

AudioCommand
├── PlayBGM
├── StopBGM
├── PlaySE
├── PlayVoice
└── SetVolume
```

游戏脚本：

```text
Script
 ↓
VM
 ↓
Command
 ↓
Subsystem
```

而不是：

```text
Script
 ↓
直接操作 Metal / AVFoundation
```

---

# 二十、Resource System

公共资源系统：

```text
ResourceManager
├── File
├── Archive
├── Image
├── Text
├── Audio
├── Video
└── Font
```

统一流程：

```text
游戏文件
   ↓
ResourceManager
   ↓
Engine-specific Parser
   ↓
Common Resource
```

引擎私有资源解析：

```text
KR → engine/kr/resource.rs
AR → engine/ar/resource.rs
ONS → engine/ons/resource.rs
```

---

# 二十一、文件系统

模拟器需要处理：

```text
目录
文件
归档
虚拟文件
资源缓存
```

建议建立：

```text
VirtualFileSystem
```

让 Engine 不直接依赖 macOS 文件 API。

例如：

```text
Engine
 ↓
VirtualFileSystem
 ↓
实际文件 / Archive
```

这样方便处理：

```text
普通目录
压缩包
嵌套资源
虚拟资源
```

---

# 二十二、C++ 架构

C++ 主要用于：

```text
已有代码复用
高性能解析
底层格式处理
图像处理
音频处理
特殊 Engine 代码
```

推荐：

```text
C++17 / C++20
CMake
```

目录：

```text
cpp/
├── common/
├── kr/
├── ar/
├── ons/
├── parser/
├── image/
└── audio/
```

---

# 二十三、Rust ↔ C++ 使用 CXX

推荐：

```text
Rust
   ↓
CXX bridge
   ↓
C++
```

原则：

> CXX 用于稳定、明确的底层 API，不要把整个 C++ 类型系统暴露给 Rust。

例如：

```text
Rust
 ↓
KrArchive
 ↓
C++
```

而不是：

```text
Rust
 ↓
整个 C++ Game Engine
```

---

# 二十四、C++ 的职责边界

C++ 不负责：

```text
Emulator 生命周期
Engine Manager
游戏主循环
UI
macOS Window
```

C++ 主要负责：

```text
Parser
Decoder
Archive
Native Library
Algorithm
```

Rust 负责：

```text
什么时候调用
调用什么
如何组合
如何驱动模拟器
```

---

# 二十五、Swift ↔ Rust

Swift 与 Rust 的业务调用采用：

```text
UniFFI
```

Swift 主要看到：

```text
Emulator
Game
GameMetadata
EmulatorSettings
SaveInfo
```

而不是：

```text
KrVM
ArOpcode
OnsInstruction
```

也就是说：

> Swift 不能知道过多 Engine 内部细节。

---

# 二十六、FFI API 设计原则

Swift API 应该尽量保持高层。

例如：

```text
loadGame(path)
start()
pause()
resume()
stop()
save()
loadSave()
setVolume()
setVideoMode()
```

而不是：

```text
executeOpcode()
modifyVMStack()
setProgramCounter()
```

后者属于 Rust Core 内部调试接口。

---

# 二十七、不要让 FFI 承担大量数据传输

错误设计：

```text
Rust
 ↓
RGBA Buffer
 ↓
Swift
 ↓
Metal
```

每帧传输大量数据会产生：

```text
CPU Copy
Memory Copy
FFI Overhead
GPU Upload
```

正确方向：

```text
Rust / wgpu
     ↓
GPU Resource
     ↓
Metal
     ↓
Screen
```

FFI 主要用于：

```text
控制
状态
配置
事件
生命周期
```

---

# 二十八、Renderer

推荐：

```text
wgpu
```

整体：

```text
Engine
   ↓
RenderCommand
   ↓
Renderer
   ↓
wgpu
   ↓
Metal
   ↓
Apple GPU
```

这样核心不绑定 Metal。

---

# 二十九、为什么不用纯 Metal

如果项目只考虑 macOS：

```text
Metal
```

完全可以。

但是当前架构更建议：

```text
wgpu
```

原因：

未来可以：

```text
macOS → Metal
Windows → DX12
Linux → Vulkan
Android → Vulkan
```

这样 Engine Core 不需要针对不同平台重复实现渲染逻辑。

---

# 三十、macOS 游戏画面

macOS：

```text
SwiftUI
   ↓
NSViewRepresentable
   ↓
MTKView
   ↓
CAMetalLayer
   ↓
Metal
```

`MTKView` 作为游戏画面承载容器。

Swift 层负责：

```text
Window
View
Resize
Fullscreen
Focus
```

Rust Renderer 负责：

```text
Texture
Buffer
Pipeline
Shader
Render Pass
```

---

# 三十一、逻辑分辨率

游戏应该拥有自己的逻辑分辨率。

例如：

```text
Game Resolution
1280 × 720
```

窗口可能是：

```text
2560 × 1440
```

渲染：

```text
Logical Resolution
        ↓
Scaling
        ↓
Viewport
        ↓
Window
```

支持：

```text
保持宽高比
整数缩放
自由缩放
黑边
Retina
全屏
超宽屏
```

---

# 三十二、Renderer Pipeline

基础流程：

```text
Engine
  ↓
Scene
  ↓
RenderCommand
  ↓
Batch
  ↓
Texture
  ↓
Render Pass
  ↓
Post Process
  ↓
Present
```

第一版只需要：

```text
Background
Sprite
Text
```

后期再增加：

```text
Shader Effect
Transition
Blur
Color Correction
CRT
Sharpen
```

---

# 三十三、游戏循环

推荐：

```text
Input
 ↓
Update
 ↓
VM
 ↓
Engine Logic
 ↓
Audio Update
 ↓
Render
 ↓
Present
```

第一版重点：

```text
稳定
正确
低输入延迟
```

之后再加入：

```text
Frame Pacing
VSync
Frame Limit
Fixed Timestep
```

---

# 三十四、Input 系统

macOS：

```text
Keyboard
Mouse
Trackpad
Gamepad
```

统一转成：

```text
InputEvent
```

流程：

```text
macOS Event
    ↓
InputController
    ↓
InputEvent
    ↓
Rust
    ↓
Engine
    ↓
VM
```

---

# 三十五、输入映射

统一：

```text
Advance
Back
Menu
Skip
Auto
Save
Load
```

可以绑定：

```text
Mouse
Keyboard
Gamepad
```

建立：

```text
InputMapper
```

管理：

```text
Default Mapping
Custom Mapping
Per Game Mapping
```

---

# 三十六、Audio 系统

统一抽象：

```text
AudioEngine
├── BGM
├── Voice
├── SE
└── Movie
```

内部命令：

```text
PlayBGM
StopBGM
PauseBGM
ResumeBGM
PlaySE
PlayVoice
SetVolume
```

macOS 后端：

```text
AVAudioEngine
AudioToolbox
```

Rust Engine 不直接访问 macOS Audio API。

---

# 三十七、Video 系统

优先：

```text
AVFoundation
```

负责：

```text
OP
ED
Movie
游戏内视频
```

遇到特殊格式时：

```text
FFmpeg
```

作为备用解码方案。

---

# 三十八、Save 系统

需要区分：

## 游戏原生存档

```text
Game Save
```

由具体 Engine 处理。

## 模拟器 Save State

```text
Emulator State
```

保存：

```text
VM
Variables
Stack
PC
Scene
Runtime State
```

第一阶段：

> 只优先实现游戏正常存档。

Save State 作为后续功能。

---

# 三十九、Game Library

macOS 应用提供：

```text
Game Library
```

Game 数据：

```text
Game
├── ID
├── Title
├── Path
├── Engine
├── Version
├── Cover
├── Icon
├── Last Played
└── Metadata
```

流程：

```text
游戏目录
   ↓
扫描
   ↓
Engine Detector
   ↓
Game Metadata
   ↓
Game Library
```

---

# 四十、配置系统

配置路径：

```text
~/Library/Application Support/GalEmu/
```

推荐：

```text
config/
games/
saves/
states/
cache/
logs/
```

设置包括：

```text
窗口大小
渲染模式
音量
快捷键
语言
全屏
过滤器
Engine 设置
```

配置格式：

```text
JSON / TOML
```

---

# 四十一、日志系统

Rust 推荐：

```text
tracing
```

等级：

```text
TRACE
DEBUG
INFO
WARN
ERROR
```

模块：

```text
Emulator
Engine
VM
Resource
Renderer
Audio
Input
Save
FFI
```

例如：

```text
[INFO] Loading game
[INFO] Detected engine: KR
[DEBUG] Loading script
[DEBUG] Opcode: ...
[INFO] Renderer initialized
```

---

# 四十二、错误处理

Rust 统一：

```text
Result<T, EmulatorError>
```

错误类别：

```text
GameNotFound
UnsupportedEngine
InvalidArchive
InvalidScript
ParserError
VMError
ResourceError
RendererError
AudioError
InputError
SaveError
FFIError
```

对于用户可见错误：

```text
Rust Error
   ↓
FFI
   ↓
Swift
   ↓
Alert / Error View
```

---

# 四十三、线程模型

初版：

```text
Main Thread
├── SwiftUI
└── AppKit

Game Thread
├── Engine
├── VM
└── Game Logic

Render
└── GPU

Audio
└── Audio Backend
```

后期根据性能需求再增加：

```text
IO Thread
Resource Decode Thread
Worker Pool
```

不要在 MVP 阶段建立大量线程。

---

# 四十四、性能设计

优先级：

```text
正确性
↓
稳定性
↓
减少数据复制
↓
缓存
↓
GPU Batch
↓
线程优化
↓
SIMD
```

重点避免：

```text
每帧大量 FFI
每帧 CPU → GPU 大量复制
重复解析同一个资源
重复加载纹理
频繁创建对象
```

---

# 四十五、测试体系

测试分四层。

## 45.1 Unit Test

Rust：

```text
Parser
Opcode
VM
Resource
Save
Detector
```

## 45.2 Engine Test

分别：

```text
KR
AR
ONS
```

测试：

```text
Detect
Load
Parse
Run
Render
Input
Save
```

## 45.3 Integration Test

测试完整流程：

```text
Game Path
 ↓
Detector
 ↓
Engine
 ↓
VM
 ↓
Render
```

## 45.4 UI Test

Swift：

```text
Game Library
Settings
Controller
Window
Save Manager
```

使用：

```text
XCTest
```

---

# 四十六、测试游戏样本

建立：

```text
testdata/
├── kr/
├── ar/
└── ons/
```

每个 Engine 准备：

```text
最小游戏样本
资源样本
脚本样本
异常样本
```

用于测试：

```text
解析
VM
资源
渲染
```

---

# 四十七、开发者模式

后期加入：

```text
Developer Mode
```

可以显示：

```text
FPS
Engine
VM PC
Current Opcode
Current Script
Memory
Texture Count
Draw Calls
Audio Channels
Game State
```

对于逆向和兼容实现尤其重要。

---

# 四十八、Engine Debugger

因为项目本身就是兼容实现，因此建议后期加入：

```text
Engine Debugger
```

功能：

```text
当前脚本
当前指令
PC
变量
Stack
Scene
Resource
```

最终可以实现：

```text
Pause
Step
Step Over
Resume
```

这会极大方便 KR / AR / ONS 的兼容调试。

---

# 四十九、macOS 文件结构

模拟器自己的数据放在：

```text
~/Library/Application Support/GalEmu/
```

结构：

```text
GalEmu/
├── config/
├── games/
├── saves/
├── states/
├── cache/
└── logs/
```

不要把用户游戏文件复制进入 App Bundle。

---

# 五十、权限设计

macOS 文件访问需要考虑：

```text
Sandbox
Security Scoped Bookmark
```

如果 App 使用 Sandbox：

```text
用户选择游戏目录
       ↓
Security Scoped Bookmark
       ↓
保存访问权限
```

这样应用重启后仍然可以访问用户授权的游戏目录。

---

# 五十一、构建体系

采用三套构建工具：

```text
Cargo
CMake
SwiftPM / Xcode
```

职责：

```text
Cargo
↓
Rust

CMake
↓
C++

Xcode / SwiftPM
↓
Swift / macOS App
```

不要让其中一个工具强行管理整个项目。

---

# 五十二、构建关系

最终：

```text
CMake
  ↓
C++ Library
  ↓
CXX
  ↓
Rust Core
  ↓
UniFFI
  ↓
Swift
  ↓
GalEmu.app
```

---

# 五十三、Build Configuration

至少准备：

```text
Debug
Release
```

Debug：

```text
详细日志
断言
开发者模式
调试功能
```

Release：

```text
优化
裁剪日志
签名
Notarization
```

---

# 五十四、Apple Silicon

开发阶段优先：

```text
arm64
```

最终：

```text
arm64
+
x86_64
```

生成：

```text
Universal macOS App
```

但如果第三方依赖无法支持 Intel，可以先发布：

```text
Apple Silicon
```

版本。

---

# 五十五、macOS 最低版本

第一阶段建议：

```text
macOS 13+
```

后续根据实际 API 使用情况决定是否提高最低版本。

核心代码不应该依赖特定 macOS 版本。

---

# 五十六、CI/CD

GitHub Actions：

```text
Rust Check
Rust Test
C++ Build
C++ Test
Swift Build
Integration Test
macOS Package
```

最终：

```text
Build
↓
Test
↓
Package
↓
Sign
↓
Notarize
↓
Release
```

---

# 五十七、代码规范

Rust：

```text
rustfmt
clippy
cargo test
```

C++：

```text
clang-format
clang-tidy
GoogleTest
```

Swift：

```text
SwiftFormat
SwiftLint
XCTest
```

PR 最少要求：

```text
Format
Lint
Test
Build
```

全部通过。

---

# 五十八、Git 分支

建议：

```text
main
develop
```

功能：

```text
feature/core
feature/kr
feature/ar
feature/ons
feature/renderer
feature/audio
feature/macos-ui
feature/save
```

提交格式：

```text
feat:
fix:
refactor:
perf:
test:
docs:
build:
```

---

# 五十九、开发阶段

## Phase 0：项目初始化

完成：

```text
[ ] Git Repository
[ ] Cargo
[ ] CMake
[ ] Xcode
[ ] SwiftUI
[ ] CXX
[ ] UniFFI
```

验证：

```text
Swift
 ↓
Rust
 ↓
C++
```

链路能够正常运行。

---

# 六十、Phase 1：Renderer

完成：

```text
[ ] wgpu
[ ] Metal Backend
[ ] Texture
[ ] Sprite
[ ] Background
[ ] Text
[ ] Resize
[ ] Fullscreen
```

验收：

```text
1280 × 720
60 FPS
```

稳定运行。

---

# 六十一、Phase 2：Resource System

完成：

```text
[ ] File System
[ ] Virtual File System
[ ] Archive
[ ] Image
[ ] Text
[ ] Audio
[ ] Video
```

建立统一：

```text
ResourceManager
```

---

# 六十二、Phase 3：KR Engine

完成：

```text
[ ] Detector
[ ] Loader
[ ] Parser
[ ] VM
[ ] Opcode
[ ] Resource
[ ] Renderer
[ ] Input
[ ] Audio
[ ] Save
```

验收目标：

```text
真实 KR 游戏
↓
启动
↓
标题
↓
开始游戏
↓
文本
↓
图片
↓
音频
↓
场景切换
↓
存档
```

---

# 六十三、Phase 4：AR Engine

完成：

```text
[ ] Detector
[ ] Loader
[ ] Parser
[ ] VM
[ ] Opcode
[ ] Resource
[ ] Renderer
[ ] Input
[ ] Audio
[ ] Save
```

重点：

> 验证 Engine Interface 是否真正合理。

要求：

```text
增加 AR
```

时不能大规模修改：

```text
Emulator
Renderer
Input
Audio
```

---

# 六十四、Phase 5：ONS Engine

完成：

```text
[ ] Detector
[ ] Loader
[ ] Parser
[ ] VM
[ ] Opcode
[ ] Resource
[ ] Renderer
[ ] Input
[ ] Audio
[ ] Save
```

ONS 接入后：

```text
KR
AR
ONS
```

三种实现共同运行。

---

# 六十五、Phase 6：Game Library

完成：

```text
[ ] Game Scanner
[ ] Engine Detector
[ ] Game Metadata
[ ] Game Cover
[ ] Recent Games
[ ] Favorites
[ ] Search
```

---

# 六十六、Phase 7：macOS Native Experience

完成：

```text
[ ] Window
[ ] Fullscreen
[ ] Menu
[ ] Shortcut
[ ] Drag & Drop
[ ] File Dialog
[ ] Settings
[ ] Save Manager
[ ] Game Library
```

---

# 六十七、Phase 8：高级功能

后续：

```text
[ ] Save State
[ ] Quick Save
[ ] Quick Load
[ ] Fast Forward
[ ] Auto Mode
[ ] Skip Mode
[ ] Text Speed
[ ] Shader
[ ] Texture Filter
[ ] Developer Mode
[ ] Engine Debugger
```

---

# 六十八、Phase 9：性能优化

重点：

```text
[ ] Renderer Batch
[ ] Texture Cache
[ ] Resource Cache
[ ] Script Cache
[ ] IO Optimization
[ ] Frame Pacing
[ ] Memory Optimization
```

---

# 六十九、Phase 10：跨平台准备

核心保持：

```text
Rust
```

尽量做到：

```text
Engine
VM
Resource
Renderer Abstraction
Audio Abstraction
Input Abstraction
```

不依赖：

```text
Swift
AppKit
macOS
```

未来可以增加：

```text
Windows Frontend
Linux Frontend
Android Frontend
```

---

# 七十、当前明确支持范围

第一阶段严格限制：

```text
Supported Engines
├── KR
├── AR
└── ONS
```

暂不纳入：

```text
其他未确定引擎
```

不为了未知引擎提前设计大量接口。

原则：

> 先让 KR / AR / ONS 真正跑起来，再根据三个引擎之间的实际差异提炼公共接口。

---

# 七十一、Engine 抽象的核心原则

不要追求：

```text
三个 Engine 代码完全相同
```

而追求：

```text
三个 Engine 可以被同一个 Emulator Runtime 管理
```

例如：

```text
Emulator
    │
    ▼
Engine
    │
    ├── KR
    ├── AR
    └── ONS
```

每个 Engine 可以拥有自己的：

```text
VM
Parser
Opcode
Resource
Scene
Save
```

只共享真正公共的内容。

---

# 七十二、判断架构是否成功的标准

第一个标准：

```text
KR 可以稳定运行
```

第二个标准：

```text
AR 可以接入
```

第三个标准：

```text
ONS 可以接入
```

最重要的是：

```text
加入 AR
```

以及：

```text
加入 ONS
```

时，不应该不断修改：

```text
Emulator
Swift UI
Renderer
Audio
Input
```

如果只需要：

```text
新增 Engine
新增 Parser
新增 VM
新增 Resource
```

说明架构基本成功。

---

# 七十三、核心依赖原则

必须尽量保持：

```text
Swift
  ↓
Rust
  ↓
C++
```

而不是：

```text
Swift
 ↓
C++
 ↓
Rust
 ↓
Swift
```

也不要形成循环依赖。

推荐：

```text
macOS
   │
   ▼
Swift
   │
   ▼
Rust Core
   │
   ▼
C++ Library
```

---

# 七十四、渲染依赖原则

```text
Engine
  ↓
RenderCommand
  ↓
Renderer
  ↓
wgpu
  ↓
Metal
```

Engine 不允许：

```text
直接操作 Metal
```

这样未来才能保留跨平台能力。

---

# 七十五、音频依赖原则

```text
Engine
 ↓
AudioCommand
 ↓
AudioEngine
 ↓
Platform Backend
```

macOS：

```text
AVAudioEngine
```

未来其他平台可以有：

```text
Windows Audio Backend
Linux Audio Backend
Android Audio Backend
```

---

# 七十六、输入依赖原则

```text
Platform Input
      ↓
InputEvent
      ↓
Engine
```

而不是：

```text
KR
 ↓
NSEvent
```

这样 KR / AR / ONS 都不需要知道自己运行在哪里。

---

# 七十七、日志与诊断原则

任何引擎问题都必须能够回答：

```text
当前 Engine 是谁？
当前脚本是什么？
当前 PC 是多少？
当前 Opcode 是什么？
当前 Scene 是什么？
当前 Resource 是什么？
```

因此：

```text
Logging
+
Developer Mode
+
Engine Debugger
```

属于本项目非常重要的开发设施。

---

# 七十八、安全原则

模拟器运行的是：

```text
未知游戏文件
```

因此解析器默认按照“不可信输入”设计。

必须防范：

```text
越界读取
整数溢出
超大资源
损坏归档
非法图片
非法音频
死循环
异常脚本
无限递归
```

VM 可以加入：

```text
Instruction Limit
Watchdog
```

避免错误游戏脚本把整个程序卡死。

---

# 七十九、最终架构

```text
                           GalEmu
                              │
                  ┌───────────┴───────────┐
                  │                       │
                  ▼                       ▼
             macOS Frontend          Rust Core
                  │                       │
            ┌─────┴─────┐        ┌──────┼──────┐
            │           │        │      │      │
         SwiftUI      AppKit   Engine   VM   Resource
            │           │        │
            │           │   ┌────┼────┐
            │           │   │    │    │
            │           │  KR   AR   ONS
            │           │
            └─────┬─────┘
                  │
               UniFFI
                  │
                  ▼
              Rust Core
                  │
             ┌────┴────┐
             │         │
            CXX       wgpu
             │         │
             ▼         ▼
            C++      Metal
```

---

# 八十、最终技术栈

## UI

```text
SwiftUI
AppKit
```

## Core

```text
Rust
Cargo
```

## Engines

```text
KR
AR
ONS
```

## C++ Integration

```text
C++17 / C++20
CMake
CXX
```

## Swift Integration

```text
UniFFI
```

## Graphics

```text
wgpu
Metal
WGSL
MTKView
CAMetalLayer
```

## Audio

```text
AVAudioEngine
AudioToolbox
```

## Video

```text
AVFoundation
FFmpeg
```

## Testing

```text
cargo test
XCTest
GoogleTest
```

## Build

```text
Cargo
CMake
SwiftPM
Xcode
```

## CI

```text
GitHub Actions
```

---

# 八十一、MVP 最终验收标准

第一版至少达到：

```text
[ ] macOS App 可以启动
[ ] SwiftUI UI 正常
[ ] Rust Core 正常加载
[ ] Rust ↔ Swift FFI 正常
[ ] Rust ↔ C++ FFI 正常
[ ] wgpu 正常运行
[ ] Metal Backend 正常运行
[ ] 游戏目录选择正常
[ ] Engine Detector 正常
[ ] KR 可以加载
[ ] KR 可以运行
[ ] KR 文本正常
[ ] KR 图片正常
[ ] KR 音频正常
[ ] KR 输入正常
[ ] KR 存档正常
```

之后：

```text
[ ] AR 加入
[ ] ONS 加入
```

并验证公共架构。

---

# 八十二、长期目标

最终形成：

```text
                    GalEmu
                      │
            ┌─────────┴─────────┐
            │                   │
         Frontend              Core
            │                   │
         macOS UI          Rust Runtime
                                │
                    ┌───────────┼───────────┐
                    │           │           │
                   KR          AR          ONS
                    │           │           │
                    └───────────┼───────────┘
                                │
                         Common Runtime
                                │
                 ┌──────────────┼──────────────┐
                 │              │              │
              Renderer        Audio          Input
                 │              │              │
                wgpu         Backend         Event
                 │
               Metal
```

核心目标不是把三个引擎强行做成同一种实现，而是建立一个：

> **可以稳定承载 KR、AR、ONS 三种引擎实现的统一模拟器 Runtime。**

未来如果需要增加新的引擎，只允许在实际需求出现以后，基于已有 KR / AR / ONS 的共性继续扩展 `Engine` 抽象，而不是提前为了大量未知引擎制造复杂架构。

---

# 八十三、项目架构最终原则

整个项目长期遵循以下原则：

1. **SwiftUI 负责应用 UI。**
2. **AppKit 负责 macOS 原生能力。**
3. **Rust 是模拟器核心。**
4. **KR / AR / ONS 是当前唯一规划的 Engine。**
5. **每个 Engine 保持独立实现。**
6. **公共层只抽取真实共性。**
7. **C++ 作为底层原生库。**
8. **CXX 负责 Rust/C++ 交互。**
9. **UniFFI 负责 Rust/Swift 高层 API。**
10. **wgpu 负责跨平台 GPU 抽象。**
11. **Metal 是 macOS GPU Backend。**
12. **VM 不依赖 macOS。**
13. **Engine 不直接依赖 Swift/AppKit。**
14. **Renderer 不直接暴露给 Engine。**
15. **Audio 不直接暴露平台 API 给 Engine。**
16. **所有平台输入统一转换为 InputEvent。**
17. **优先保证兼容性与稳定性，再进行性能优化。**
18. **先完成 KR，再使用 AR、ONS 验证架构。**
19. **第一版不做复杂插件系统。**
20. **未来跨平台能力通过 Rust Core 保留。**

---

# 八十四、推荐开发路线

最终路线固定为：

```text
项目初始化
    ↓
SwiftUI + AppKit
    ↓
Rust Core
    ↓
C++ + CMake
    ↓
Rust ↔ CXX
    ↓
Rust ↔ Swift / UniFFI
    ↓
wgpu + Metal
    ↓
Resource System
    ↓
Engine Interface
    ↓
KR
    ↓
VM
    ↓
Renderer
    ↓
Input
    ↓
Audio
    ↓
Save
    ↓
完整运行 KR
    ↓
AR
    ↓
ONS
    ↓
公共 Runtime 稳定化
    ↓
Game Library
    ↓
macOS 完整体验
    ↓
调试器
    ↓
性能优化
    ↓
跨平台
```

---

# 八十五、最终项目定位

本项目最终定位为：

> **一个以 Rust 为核心、面向 KR / AR / ONS 引擎的 Galgame Emulator Runtime，并提供原生 macOS SwiftUI/AppKit 前端。**

最终架构：

```text
SwiftUI / AppKit
        │
        ▼
macOS Frontend
        │
      UniFFI
        │
        ▼
Rust Emulator Core
        │
   ┌────┼────┐
   ▼    ▼    ▼
  KR    AR   ONS
   │    │    │
   └────┼────┘
        ▼
 Common Runtime
        │
 ┌──────┼──────┐
 ▼      ▼      ▼
VM   Resource Renderer
             │
            wgpu
             │
           Metal
```

**核心思想：**

> **Swift 负责“应用”，Rust 负责“模拟器”，KR/AR/ONS 负责“引擎兼容”，C++ 负责“底层库”，wgpu/Metal 负责“渲染”。**

这个边界作为整个项目的长期架构基准，不应因为某一个具体引擎的特殊需求而随意打破。
